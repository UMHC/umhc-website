import { MeetingLocation } from "@/types/dashboard";

export class MeetingLocationService {
    static async getActive(): Promise<MeetingLocation[]> {
        // Stub implementation to fix build
        return [];
    }
}
